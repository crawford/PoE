source [find board/efm32.cfg]

# This does not work (could not find MEM-AP)
# reset_config srst_only combined srst_nogate srst_open_drain connect_assert_srst

# This does not work (could not find MEM-AP)
# reset_config srst_only srst_nogate srst_open_drain connect_assert_srst

# This works (currently stops in the hardfault handler)
# Warn : 'srst_nogate' reset_config option is required
# reset_config srst_only srst_open_drain connect_assert_srst

# Trying this on the SLSTK3701a
reset_config srst_open_drain connect_assert_srst

tpiu config internal log.itm uart false 9500000
# tpiu create efm32.cpu.tpiu -dap efm32.dap -ap-num 0x0
# efm32.cpu.tpiu configure -protocol uart -traceclk 9500000 -output log.itm -formatter false
# efm32.cpu.tpiu enable

# adapter speed 100

init
reset halt

# mww 0xE000ED08 0x00004000

rtt setup 0x20000000 16384 "SEGGER RTT"
rtt start
rtt server start 9090 0
rtt server start 9091 1